#include <iostream>

using namespace std;

int main_15(){

    int x = 5;
    int y = 3;
    cout << (x < y) << endl; // returns 0 (true) because 5 is lesser than 3
    cout << (x > y) << endl; // return 1 (False) because 5 is greater than 3
    return 0;
}